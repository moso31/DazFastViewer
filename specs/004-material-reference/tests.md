# 验证约定

轻量回归：`scene_ir_dson` 与 `camera_mailbox`；新增参数应包含非有限值、纹理索引、单位转换与通道覆盖的针对性检查。

两份真实资产各执行一次必要的 Blender 参考与 Runtime 后台渲染，固定分辨率、采样、设备和几何。遇到明确故障才重跑对应步骤。记录基础顶点误差、材质绑定、参考节点、线性图像差异及渲染设置。

材质 Delta：同一 Session 加载一次，渲染前后改变可见材质颜色；检查像素变化、材质更新计数、网格指针与三角形数保持一致。此项不等同 GPU 上传字节计数验收。

执行结果见 [执行报告](execution-report.md)：两项 CTest、两资产基础结构与道具材质 Delta 均通过；两份图像差异已记录，完整材质 Golden 未验收。GPU 上传字节为 NOT_MEASURED；未运行的其他项目继续保留 NOT_RUN，不将工具成功退出视为视觉一致。
