# 007：公式依赖、ERC 与 JCM

用户已授权先归档 006，再实施 007，并要求完成后复测 005 的 Morph 参数启用情况。006 已于本轮中文提交为 `aa8fbc7`；007 开发继续使用当前模型与既有 C++ / Qt / Cycles 架构。

本阶段将参数目录中的 Formula、布尔开关和子节点 Alias 接入统一求值图，输出可同时驱动其他 Morph、骨骼变换、绑定中心 / 终点与 orientation。JCM 根据骨骼通道及资源开关自动求值，位于蒙皮之前。骨骼缩放的双四元数扩展也是本阶段必需内容，否则 Arms Length 与 Chest Scale 无法实际使用。

验收重点为 G8 指定参数的 0 / 0.5 / 1 变形、恢复、Alias 双向共享、布尔默认开启语义、自动 JCM、006 的 40 个姿势及副屏真实滑块操作。G8.1 单独核对；本代空覆盖仍须保留，不能为了增加“点亮数量”强行启用被替换的旧表情。

完整 DAZ Studio Golden、HD / SubD、FitTo、FK / IK 与动态服装不在此次保证范围。未解析、循环或不支持的资源须明确报告，不能以某个第三方坏依赖阻断整个角色。
